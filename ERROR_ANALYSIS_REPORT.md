# Next.js 빌드 오류 분석 보고서

## 📋 오류 정보

**오류 메시지:**
```
useSearchParams() should be wrapped in a suspense boundary at page "/auth/login"
```

**발생 위치:**
- 파일: `app/auth/login/page.tsx`
- 라인: 16 (`const searchParams = useSearchParams();`)

**빌드 단계:**
- TypeScript 컴파일: ✅ 성공
- 정적 페이지 생성 (SSG): ❌ 실패

---

## 🔍 원인 분석

### 1. 기술적 원인

**Next.js App Router의 동작 방식:**
- Next.js 13+ App Router는 기본적으로 **정적 생성(Static Site Generation, SSG)**을 시도합니다
- 빌드 시점에 모든 페이지를 미리 생성하려고 합니다

**`useSearchParams()`의 특성:**
- `useSearchParams()`는 **클라이언트 사이드에서만 작동**하는 훅입니다
- URL의 쿼리 파라미터를 읽기 위해 브라우저 환경이 필요합니다
- 서버 사이드(빌드 시점)에서는 URL 파라미터를 알 수 없습니다

**충돌 발생:**
- Next.js가 빌드 시점에 `/auth/login` 페이지를 정적으로 생성하려고 시도
- 하지만 `useSearchParams()`는 서버에서 실행할 수 없음
- 결과: 빌드 실패

### 2. 왜 이 오류가 발생했는가?

**코드 구조:**
```typescript
export default function LoginPage() {
  const searchParams = useSearchParams(); // ❌ 문제 발생 지점
  // ...
}
```

**문제점:**
1. 컴포넌트 최상위 레벨에서 `useSearchParams()` 직접 호출
2. Suspense boundary 없이 사용
3. Next.js가 정적 생성 시도 시 오류 발생

### 3. 영향 범위

**영향받는 파일:**
- ✅ `app/auth/login/page.tsx` - 오류 발생
- ⚠️ `app/auth/signup/page.tsx` - 동일한 패턴 사용 (예상 오류)

---

## 💡 해결 방안

### 해결 방안 1: Suspense Boundary 사용 (권장)

**장점:**
- Next.js 권장 방법
- 정적 생성과 동적 렌더링 모두 지원
- 성능 최적화 유지

**구현 방법:**
1. `useSearchParams()`를 사용하는 부분을 별도 컴포넌트로 분리
2. 해당 컴포넌트를 `<Suspense>`로 감싸기
3. 로딩 상태 처리

### 해결 방안 2: 동적 렌더링 강제

**장점:**
- 구현이 간단
- 즉시 해결 가능

**단점:**
- 정적 생성의 이점 포기
- 성능 최적화 기회 상실

**구현 방법:**
- `export const dynamic = 'force-dynamic'` 추가

### 해결 방안 3: 클라이언트 사이드에서만 처리

**장점:**
- 서버 사이드 렌더링 문제 완전 회피

**단점:**
- 초기 로딩 시 에러 메시지 표시 지연

**구현 방법:**
- `useEffect`와 `window.location.search` 사용

---

## ✅ 권장 해결책

**해결 방안 1 (Suspense Boundary)**을 권장합니다.

**이유:**
1. Next.js 공식 권장 방법
2. 성능 최적화 유지
3. 사용자 경험 개선 (로딩 상태 표시 가능)
4. 확장성 좋음

---

## 🔧 구현 계획

1. `useSearchParams()` 사용 부분을 별도 컴포넌트로 분리
2. `<Suspense>`로 감싸기
3. 로딩 fallback 추가
4. 동일한 패턴을 `signup/page.tsx`에도 적용

---

## 📊 예상 결과

**수정 후:**
- ✅ 빌드 성공
- ✅ 정적 생성 가능
- ✅ 동적 콘텐츠도 정상 작동
- ✅ 성능 최적화 유지

---

**보고서 작성일:** 2025-01-01  
**분석자:** AI Assistant  
**우선순위:** 높음 (빌드 실패로 배포 불가)

